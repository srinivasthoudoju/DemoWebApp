name: A workflow for my demo website
on:
 workflow_dispatch
jobs:
 build-job:
  name: Hello world action
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v1
  - name: Hello world
    run: echo Hello world
  - name: print html content
    run: |
     cat index.html
     cat courses.html
  
    
  
  
